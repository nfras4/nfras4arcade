# nfras4arcade - Multiplayer Party Games Platform

## Project Overview
nfras4arcade is a multi-game party platform with seven games:
- **Impostor** — Social deduction game where one player is secretly the "impostor" who doesn't know the secret word but gets a vague hint. Players give clues and vote.
- **President** — Multiplayer card game (climb/shedding style).
- **Chase the Queen** — Card passing game where players avoid the queen of spades.
- **Connect 4** — Classic 2-player strategy game on a 7×6 grid with multi-round scoring.
- **Wavelength** — Team-based party game where players guess where a concept falls on a spectrum.
- **Texas Hold'em Poker** — RETIRED 2026-05-12. Server code remains dormant (DO binding kept, /ws/poker returns 503, /poker page shows retired notice). Removed from casino hub. Code lives in `worker/poker/` for archaeology but is unreachable from the UI. See deploy log 2026-05-01 for the original DO usage runaway that prompted the kill switch.
- **Snap** — Real-time card-matching race where players slap matching cards as fast as possible.

All card games (President, Chase the Queen, Connect 4, Texas Hold'em Poker) extend a shared `CardRoom` base Durable Object class. Snap and Wavelength are standalone Durable Objects.

## Tech Stack
- **Frontend:** SvelteKit 5 (runes mode) with `adapter-cloudflare`
- **Backend:** Cloudflare Workers + Durable Objects (WebSocket Hibernation API)
- **Database:** Cloudflare D1 (SQLite)
- **Auth:** PBKDF2-SHA256 via Web Crypto, cookie-based sessions (7-day expiry)
- **Package Manager:** Bun (always use `bun add`, never `npm install`)

## Architecture

```
src/                        # SvelteKit frontend
  lib/
    types.ts                # Shared TypeScript types (Player, GameState, messages)
    ws.ts                   # WebSocket client singleton (supports guest mode)
    cardSocket.ts           # WebSocket client for card games (supports guest mode)
    stores.ts               # Svelte stores + message handlers
    auth.ts                 # Auth client (login, register, fetchUser)
    guest.ts                # Guest identity (localStorage 'arcade-guest-id', shared across tabs; same-browser multi-tab multiplayer is NOT possible)
    server/auth/
      password.ts           # PBKDF2-SHA256 password hashing (Web Crypto)
      session.ts            # Session management (D1 + cookies)
    components/
      FeedbackWidget.svelte # Global feedback button + modal
      poker/                # Poker UI components (PokerTable, PlayerSeat, BettingControls, HandDisplay)
      snap/                 # Snap UI components (CardStack, CenterPad, RaceOverlay)
  routes/
    +layout.svelte          # Global nav + FeedbackWidget
    +page.svelte            # nfras4arcade hub (game cards, guest banner)
    impostor/
      +page.svelte          # Impostor lobby (create/join room)
      [code]/+page.svelte   # Main game UI (all phases incl. post-game)
    president/              # President card game
    chase-the-queen/        # Chase the Queen card game
    connect-four/
      +page.svelte          # Connect 4 lobby (create/join)
      [code]/+page.svelte   # Game board UI (interactive grid, hover preview, scores)
      tutorial/+page.svelte # Rules and strategy tips
    poker/
      +page.svelte          # Poker lobby (create/join, chip balance display)
      [code]/+page.svelte   # Game table UI (hole cards, community cards, betting controls)
    snap/
      +page.svelte          # Snap lobby (create/join)
      [code]/+page.svelte   # Game UI (center pad, card stack, race resolution)
    login/+page.svelte      # Login form + "Continue as Guest"
    register/+page.svelte   # Registration form + "Continue as Guest"
    profile/+page.svelte    # Player profile, stats, badges
    admin/
      feedback/             # Admin feedback viewer (sortable table)
    api/                    # SvelteKit server routes
      auth/                 # Auth endpoints (login, register, logout, me, profile)
      create/               # Room code generation (no auth required)
      create-solo/          # Solo game creation with bots
      feedback/             # Feedback submission endpoint
      categories/           # Word categories list
      room/[code]/          # Room info via DO
worker/
  impostor/
    room.ts                 # ImpostorRoom Durable Object (reconnect, guest, leave)
    types.ts                # DO state types
    words.ts                # Word bank (8 categories)
  cards/
    cardRoom.ts             # Base CardRoom DO (abstract, shared card game logic + badge awarding)
    president.ts            # PresidentRoom DO
    chaseTheQueen.ts        # ChaseTheQueenRoom DO
  connectFour/
    room.ts                 # ConnectFourRoom DO (extends CardRoom, 7×6 grid, win/draw detection)
  poker/
    room.ts                 # PokerRoom DO (extends CardRoom, betting rounds, hand eval, chip persistence)
    handEvaluator.ts        # Hand ranking logic (royal flush → high card, side pot splitting)
    potCalculator.ts        # Pot and side pot calculation for all-in scenarios
  snap/
    room.ts                 # SnapRoom DO (standalone, multi-device roles, real-time race resolution)
  bots/
    connectFourBot.ts       # Connect 4 bot AI (win/block/center priority chain)
    pokerBot.ts             # Poker bot AI (hand strength estimation, betting strategy)
    # + other bot files for card games
  index.ts                  # Placeholder (overwritten by adapter-cloudflare)
scripts/
  patch-worker.ts           # Post-build: patches worker with DO export + WS upgrade + guest auth
migrations/
  0001_initial.sql          # D1 schema (users, sessions, profiles, badges, game_sessions)
  0002_lone_monkey_badge.sql # Badge migration
  0003_feedback.sql         # Feedback table for in-game feedback system
  0004_badge_update.sql     # 6 new badges (connect_four_win, social_butterfly, card_shark, night_owl, stalemate, speed_demon)
  0005_poker_chips.sql      # Adds `chips` column to player_profiles for persistent poker currency
```

## Key Commands
```bash
bun install                 # Install dependencies
bun run dev                 # Dev mode via wrangler dev
bun run build               # Build SvelteKit + patch worker
bun run db:migrate:local    # Run D1 migrations locally
bun run db:migrate:prod     # Run D1 migrations in production
```

## Game Flow (Impostor)
1. **Lobby** - Players join via 4-letter room code, host sets category + mode
2. **Hints** - 2 standard hint rounds (+ optional 3rd). Players take turns giving text hints or speaking (voice mode)
3. **Discussion** - Host chooses: next hint round or start voting
4. **Voting** - Single-click lock-in vote for who you think is the impostor
5. **Reveal** - Shows impostor, word, hint, vote breakdown. D1 stats updated. Non-host can leave.
6. **Game Over** - Full post-game screen with player list, votes. Host clicks "Play Again" or everyone can "Leave".

## Game Flow (Connect 4)
1. **Lobby** - 2 players join via room code, can add 1 bot for solo play
2. **Playing** - Players alternate dropping pieces into columns. Hover preview shows where piece will land.
3. **Round Over** - Win detected (4 in a row horizontal/vertical/diagonal) or draw (board full). Scores update.
4. **Next Round** - First player alternates each round. Play continues until players leave.
5. **Game Over** - Final scores shown. D1 stats + badges awarded for logged-in players.

## Game Flow (Texas Hold'em Poker)
1. **Lobby** - Players join via room code; chip balance (persistent via D1 `player_profiles.chips`) shown per seat
2. **Pre-flop** - Blinds posted, hole cards dealt, first betting round (fold/call/raise)
3. **Flop / Turn / River** - Community cards revealed one stage at a time, betting round after each
4. **Showdown** - `handEvaluator.ts` ranks surviving hands; `potCalculator.ts` splits main pot and any side pots for all-in players
5. **Round End** - Chips transferred, D1 balances updated; bust-out players can rebuy or leave
6. **Architecture notes** - PokerRoom extends CardRoom (inherits deck management, reconnection, badge hooks). Betting state machine lives in `room.ts`. Hand evaluation and pot calculation are pure functions in separate files for testability.

## Game Flow (Snap)
1. **Lobby** - Players join; one device can act as the shared "center pad" display, others are player devices
2. **Dealing** - Deck split evenly; players flip cards onto a central pile in turn order
3. **Snap** - When the top two cards match, first player to tap their device wins the pile (race resolved server-side in SnapRoom DO to avoid client timing disputes)
4. **Win** - Player who collects all cards (or most cards when time runs out) wins
5. **Architecture notes** - SnapRoom is a standalone DO (no CardRoom inheritance, same pattern as Wavelength). Multi-device roles (center pad vs. player) handled via a `role` field in the WebSocket join message. Real-time race resolution uses DO request serialization to guarantee a single winner per snap event.

## Badge System (13 badges)
Badges are stored in D1 and awarded via `INSERT OR IGNORE` (idempotent). Guest players (`guest_` prefix) are skipped.

**Regular badges (visible before earning):**
- First Game, Champion (first win), Veteran (10 games), Impostor Win, Perfect Detective
- Going Bananas, Lone Monkey, Four in a Row (C4 win), Social Butterfly (all 4 game types), Card Shark (10 card wins)

**Easter egg badges (hidden until earned):**
- Night Owl (play midnight–5am UTC), Stalemate (C4 draw), Speed Demon (win in <2 minutes)

**Badge awarding architecture:**
- Shared logic in `CardRoom.recordGameEnd()` → calls `checkPostGameBadges()` for cross-game badges (veteran, night_owl, speed_demon, social_butterfly, card_shark)
- Game-specific badges in subclass methods (e.g. `awardConnectFourBadges()` for connect_four_win, stalemate)
- Profile page (`/profile`) shows emoji icons with CSS hover tooltips (name, description, earned date)

**Adding a new badge:** D1 migration INSERT → award logic in relevant DO → add to `allBadges` array in profile page

## Owner Crown
The `nfras4` account gets a crown emoji (👑) next to their name in all game lobbies. Checked client-side by display name match. CSS class: `.owner-crown`.

## Durable Object Bindings
- `IMPOSTOR_ROOM` — ImpostorRoom
- `PRESIDENT_ROOM` — PresidentRoom
- `CHASE_QUEEN_ROOM` — ChaseTheQueenRoom
- `CONNECT_FOUR_ROOM` — ConnectFourRoom
- `POKER_ROOM` — PokerRoom (extends CardRoom)
- `SNAP_ROOM` — SnapRoom (standalone DO, same pattern as Wavelength)

**WebSocket routes:** `/ws` (Impostor), `/ws/president`, `/ws/chase-the-queen`, `/ws/connect-four`, `/ws/poker`, `/ws/snap`

## Auth & Guest Mode
- Login is **optional**. All gameplay is accessible to guests.
- Guests get a session-persistent identity via `sessionStorage` (e.g. `Guest_a1b2`).
- Guest player IDs are prefixed with `guest_` — D1 stats/badges are skipped for guests.
- WebSocket auth: logged-in users authenticate via session cookie; guests pass `guestId` URL param.
- Login/register pages show "Continue as Guest" button with benefit explanation.

## Player Reconnection
- On disconnect mid-game, players get 45 seconds to reconnect (shown as "Reconnecting..." to others).
- After timeout, player is marked "Disconnected" — their turn is skipped in hints phase.
- Host promotion: if the host disconnects/leaves, the next connected player becomes host.
- If no connected players can be promoted, the lobby is dissolved with a message.
- Guests reconnect via the same `guestId` stored in `sessionStorage`.

## Feedback System
- Persistent feedback widget (bottom-right corner) available on every page via `+layout.svelte`.
- Players submit feedback with category (Bug/Suggestion/Other) + free text (max 2000 chars).
- Stored in D1 `feedback` table, tagged with player info, room code, game type, timestamp.
- Admin view at `/admin/feedback` — sortable table (TODO: add auth protection).

## Svelte 5 Runes
- **Do NOT use `onMount`/`onDestroy`** - they get tree-shaken in production builds. Use `$effect` instead.
- Always use runes syntax: `$state`, `$derived`, `$effect`, `$props`
- Use `{@render children()}` not `<slot />`

## Workers Runtime Constraints
- No Node built-ins, no native modules
- Use Web Crypto API (not oslo/bcrypt)
- No ORMs - raw D1 SQL queries only, all parameterised

## Word Categories
Clash Royale Cards, Animals, Food & Drinks, Movies & Shows, Professions, Sports, Landmarks, Video Games

## Recent deployments
- **2026-06-14** (7th) - Deployed to nfras4arcade (version `db623b31-43bb-4d31-aa79-f11db03c020f`; toast first shipped in `cedffa5d`). Summary: Barrel Night crown-award TOAST (the deferred presentation polish from the brief). When a BN game ends and the crown is awarded, `awardBarrelNightCrown` calls `this.sendTo(recipientId, { type:'crown_awarded', until })` - reaches the winner whether seated or spectating-after-elimination (sendTo is tag-based via getWebSockets(playerId)); the liars-dice client (msg is `any`, no client type change) shows a gold toast "👑 You won Barrel Night! The crown is yours for a week." for 9s, one-shot slide-in, reduced-motion respected. If the winner has left, the crown on their monkey + the /barrel-night champion card remain the fallback (no client handler needed elsewhere). New ServerMessage variant `crown_awarded`. VERIFIED ON PROD (ToastWinner acct, owner+player, since deleted): played 3 BN games to a win; the toast fired every time with the correct text. First positioning (top:4.5rem z-200) was buried under the centre game-over RITUAL banner; bumped to top-centre z-index 9999. Confirmed via in-page DOM diagnostic that the toast is position:fixed, z 9999, centred, and elementFromPoint at its centre returns its own child (nothing covering it, no transformed-ancestor trap). NOTE for future visual QA: the toast auto-dismisses after 9s, so `browser_take_screenshot` races the timer (the evaluate-to-screenshot tool latency often misses it); assert via DOM (querySelector('.crown-toast') + elementFromPoint) rather than a screenshot. tsc clean, vitest 199/199, build clean. Branch: `main`, commits: `4beb473` + `22eeba9`.
- **2026-06-14** (6th) - Deployed to nfras4arcade (version `3670eb98-f511-49f9-96e9-f5d12ad6cff5`). Summary: Barrel Night FEATURE COMPLETE - weekly cron + delayed-start lobby window. The cron opens a 10-min join window; the room's own alarm then fills bots to the table size and starts (so humans get time to arrive). (1) worker/barrelNight/cron.ts: `scheduled` handler (kill-switch gated) -> stub.fetch /bn-open {startInMs: 10min} for the current ISO week; idempotent + fail-safe. (2) LiarsDiceRoom /bn-open gained `startInMs` (schedules a start alarm, bnStartAt persisted) and `bots` now means "fill to N total" via a seedBots(target) helper; the alarm fills bots to BN_TABLE_SIZE=4 and starts when the window ends. (3) patch-worker.ts injects `worker_default.scheduled = barrelNightScheduled` (no new DO class - reuses LIARS_DICE_ROOM, no v15 tag). (4) wrangler.jsonc triggers.crons - NOTE: Cloudflare's API REJECTED numeric DOW "0 9 * * 0" (error 10100 invalid cron string); fixed to "0 9 * * SUN" which deploys (schedule confirmed registered: `0 9 * * SUN`). VERIFIED ON PROD (test acct CronCheck, since deleted): triggered /bn-open with startInMs=8s + 0 bots -> room opened in lobby (started:false, startScheduled:true, 0 players) -> ~8s later the alarm filled 4 bots + started -> bots auto-played + auto-advanced to game_over (~3.4min) -> event row status=complete, winner null (zero-human rule). The cron handler calls this exact path. BARREL_NIGHT_ENABLED stays "true". Barrel Night end-to-end is now: weekly cron (Sun 19:00 AEST) -> single liar's dice game (humans + bot fill, last-standing) -> best-placing human gets a 7-day crown that renders everywhere via the override; /barrel-night page (countdown + take-a-seat + champion) + hub strip for discovery; owner /api/barrel-night/trigger for manual runs. Both the human-crown path (prev deploy) and the bot/zero-human + cron-delayed-start paths verified live. tsc clean, vitest 199/199, build clean (scheduled injection verified). Branch: `main`, commits: `3ccda22` + `9bd75af`.
- **2026-06-14** (5th) - Deployed to nfras4arcade (version `3d602e92-43f7-417d-a238-56fdaac043ab`; page shipped in `3de6ce09`). Summary: Barrel Night /barrel-night page + status API + hub strip, AND the human-crown path verified end to end on prod. (1) worker/barrelNight/week.ts - pure UTC ISO-week + next-Sunday-0900-UTC helpers (shared by the status endpoint and the future cron so BN-{week} always agrees); 6 unit tests incl. year-boundary + Sunday cutover. (2) GET /api/barrel-night/status (public, guest-readable): nextStartIso, live game (event row for current ISO week with status != complete), lastChampion (joined to player_profiles). (3) /barrel-night page: live countdown to Sun 7pm AEST, "Take a seat" -> /liars-dice/BN-{week} when live, reigning-champion card, how-it-works; scoped styles, no Tailwind, no ambient pulse. (4) Hub gold "Barrel Night" strip (mirrors store strip) for discoverability. (5) /api/barrel-night/trigger now defaults to the current ISO week so the page's live link aligns. VERIFIED ON PROD (test account CrownSeeker, owner+player, since deleted): opened the current-week event via the owner trigger -> page flipped to LIVE NOW -> took a seat -> added a bot + started via the real UI -> played heads-up to game_over (bot won, I was the only human eliminated last) -> crown awarded to ME (best-placing-human rule): event row status=complete winner=CrownSeeker, player_profiles.crown_active_until = game_end+7d, /api/auth/me returns hat=crown (override delivering), /barrel-night shows CrownSeeker as reigning champion. Idle + live page states both screenshotted. Test account + event row purged after. BARREL_NIGHT_ENABLED left "true" (verified; nothing auto-fires it yet - only the owner trigger - until the cron lands). REMAINING (task #4): the weekly scheduled cron handler (0 9 * * 0 UTC = 19:00 AEST) that calls the same /bn-open; that's the only piece left to make it run automatically each week. tsc clean, vitest 199/199, build clean. Branch: `main`, commits: `ee2cb06` + `583e04d`.
- **2026-06-14** (4th) - Deployed to nfras4arcade (version `e9d4787b-6857-4f6e-9669-eb15f797a07d`). Summary: Barrel Night THIN SLICE (first vertical of the re-scoped, council-approved design). Barrel Night was redesigned after an LLM council (4-of-5: the 8-seat bracket + coordinator DO + match-routing was over-built for 0-3-human turnout and re-created the poker DO-cost runaway surface). Key insight: liar's dice is ALREADY a last-standing elimination game, so Barrel Night = a single scheduled liar's dice game whose winner gets the weekly crown - NO coordinator DO, NO bracket, NO match routing, NO new DO class (reuses LIARS_DICE_ROOM, so no v15 tag / no patch-worker export). Design doc: .omc/plans/barrel-night-match-routing-ux.md (council transcript + report alongside). Shipped this slice: (1) worker/barrelNight/crown.ts - pure pickBarrelNightWinner (winner-if-eligible-human, else last-human-eliminated, else null), 6 unit tests. (2) LiarsDiceRoom BN mode (additive): POST /bn-open (kill-switch gated) ensures the barrel_night_events row, seeds bots, optionally starts; tracks eligible-human (non-bot, non-guest) elimination order; AUTO-ADVANCES rounds via a bnAdvanceAt alarm (bot-only games have no human host to click next_round, so they'd otherwise stall at round_over); on game_over awards the crown IDEMPOTENTLY (UPDATE ... WHERE status != 'complete') to the best-placing human and fail-safe (errors logged never thrown); zero-human/bot weeks complete with no crown, no bot ever holds it. (3) /api/barrel-night/trigger (owner-only via locals.user.isOwner) fires /bn-open via stub.fetch - the future cron reuses this exact call. (4) BARREL_NIGHT_ENABLED kill-switch (wrangler var set "true" + Env + app.d.ts Platform.env gains LIARS_DICE_ROOM). PROVEN ON PROD: triggered a 2-bot game (week smoke1) -> it auto-played + auto-advanced to game_over on its own -> event row flipped status=complete, winner_player_id=null (zero-human rule) -> re-running the award claim returned 0 changes and did NOT overwrite (idempotency). Temp owner account + smoke event row cleaned from D1 after. NOT yet verified: the human-crown path end to end (best-placing human gets the crown) - needs a real liar's dice playthrough, do when the /barrel-night page lands. DEFERRED (tasks #4-5): scheduled cron handler (0 9 * * 0 UTC = 19:00 AEST, reuses /bn-open), /barrel-night page + /api/barrel-night/status + hub seeding strip. tsc clean, vitest 193/193, build clean. Branch: `main`, commit: `2c3f3cc`.
- **2026-06-14** (3rd) - Deployed to nfras4arcade (version `0375766b-9733-4454-a6cb-52774945d394`; intermediate `6a55e102`). Summary: Phase 4 Wave 2 PARTIAL - crown override + table badges/frame/title. Scoped per Nick: shipped the low-risk, independently-valuable half; the cron-driven Barrel Night bracket is DEFERRED (needs human auto-join design + DO-cost review, per the poker-runaway lesson - a cron spawning liars-dice DOs with alarm-driven bot turns is the exact runaway profile). Shipped: (1) migration 0030 (barrel_night_events table + player_profiles.crown_active_until column; applied to remote D1 before deploy). (2) Crown override at CosmeticsCache.get: on cache-miss, after resolvePlayerCosmetics, a nested-try SELECT crown_active_until; if > now, hatId='crown'. CLONES the payload (never mutates the shared DEFAULT_COSMETICS singleton) and on any error returns the resolved payload untouched (never DEFAULT) - so a missing column or D1 blip degrades to "no crown", never "no cosmetics". Lives at the cache layer so game DOs inherit it without the resolver hot path paying the join per state recompute. (3) Crown override ALSO added to /api/auth/me, which calls resolvePlayerCosmetics directly (NOT the cache) - without it the seated table monkey would wear the crown but the self-portrait (reads currentUser.hat from auth/me) would not. Now both agree. (4) Stage D: frame tint + emblem + title now render on the liars-dice 3D table nameplates by reusing the existing NameFrame component (size=full); threaded titleText/emblemSvg/frameSvg through PlayerViewLike + the ldStateLike route remap (same drop-bug class as the Wave 1 hat fix - the remap silently dropped fields the layer needed). URL-based svg via <img>/resolveTint, no @html. Crown is dormant in prod until the bracket sets crown_active_until. Verified live with a throwaway account (badgetest, since deleted): /api/auth/me returns hat=crown + emblem + frame + VETERAN title with crown_active_until set; on the table, nameplate shows emblem+VETERAN title, self-portrait monkey wears the gold crown (screenshots). 5 new crown-override unit tests incl. the fail-safe (D1 error returns resolved not DEFAULT) + DEFAULT-singleton no-mutation guard. DEFERRED to a focused session: BarrelNightRoom DO + cron + wrangler v15/triggers + patch-worker scheduled wiring + liarsDice BN result reporting + /barrel-night page + /customize crown caption (plan still at .omc/plans/table-phase4-brief.md Stage C). tsc clean, vitest 187/187, build clean. Branch: `main`, commits: `652ee89` + `4c5a270`.
- **2026-06-14** (2nd) - Deployed to nfras4arcade (version `df3318f5-4c44-4ebf-898e-60f4eb317535`). Summary: Phase 4 Wave 1 bug fixes found during an authenticated prod buy/equip check (Playwright, test account hattest, since deleted from D1). Four fixes, all client/endpoint (server broadcast was already correct): (1) HIGH - seated liars-dice table monkey never wore the equipped hat. The route's `ldStateLike` derived remap in `src/routes/liars-dice/[code]/+page.svelte` rebuilt each player WITHOUT `hat`, so `LiarsDiceTableLayer` always saw `'none'`; the self-portrait worked only because it reads `currentUser.hat` via `/api/auth/me`, a separate path. Fix: add `hat` to `PlayerViewLike` (core/types.ts) and to the remap. (2) MED - `/customize` always showed "No hat" checked: `/api/shop/inventory` equipped SELECT (and its fallback object) omitted `hat_id`, and `/customize` reads the loadout from that endpoint. Fix: add `hat_id` to both. (3) LOW copy - shop hat cards with null `level_requirement` rendered "Unlocks at Lv  or buy..." with a blank level; now show "Buy for N chips" / "Earn only". (4) Login "click does nothing" (real user report): `login()`/`register()` in `src/lib/auth.ts` had no try/catch around the fetch, so a failed request (network drop, or CF connection reset on bot-blocked networks) left the button stuck on "Logging in..." with no error and an unhandled rejection. Fix: wrap fetch, return a network error, make `handleSubmit` reset `loading` in a `finally`, and delete the buggy `handleKeydown` that passed a KeyboardEvent (currentTarget=input) into `new FormData(form)`. Verified live post-deploy: seated monkey wears the party hat (screenshot), /customize shows party checked, login still works. PROCESS NOTE / git hazard: a PARALLEL session checked out `security/high-pass-3` (off main at 948d970) and committed `519a265` "Security audit third pass" mid-session, switching the shared HEAD; my fix commit landed on that branch by accident. Recovered cleanly (no overlap - security touched login/+server.ts, I touched login/+page.svelte): cherry-picked my fix to main (`8876868`) and reset `security/high-pass-3` back to `519a265` to preserve the parallel work untouched. That security branch is LOCAL ONLY, unpushed, NOT deployed - its rateLimiter DO + wrangler.jsonc changes are still pending. Reaffirms [[one-session-per-repo]]. tsc clean, vitest 182/182. Branch: `main`, commit: `8876868`.
- **2026-06-14** - Deployed to nfras4arcade (version `20d6b548-b606-44f3-a4f1-436e4f5182af`). Summary: Phase 4 Wave 1 - hats cosmetics economy (Stages A+B). This was built by an autopilot run the previous night but never committed (laptop died mid-session); recovered intact from the working tree, verified, committed, and shipped. Stage A: migration `0029_hat_slot_and_seed.sql` adds `player_equipped.hat_id TEXT REFERENCES shop_items(id)` and seeds 5 hat rows (party 200c, beanie 350c, top_hat 600c, sombrero 800c level-5, crown 0c tier='hero' unbuyable); cosmetics resolver returns `hatId` via the existing equipped join; `/api/auth/me` returns `hat`; `/api/shop/equip` accepts the `'hat'` slot; `/customize` Hat tab + `/shop` Hats section. Stage B: `HatId` widened to none|party|crown|top_hat|beanie|sombrero, `PlaceholderMonkey` draws three new inline hat shapes (top_hat/beanie/sombrero), the liars-dice 3D table renders the equipped hat end to end, and `hat: p.hat ?? null` is broadcast from poker/connectFour/coup/snap/wavelength DOs (forward-compat; only liars-dice has a 3D monkey today). Known minor gap: impostor + cards/cardRoom populate `hat` but do not yet emit it in their broadcast mapper (harmless, no 3D monkey there). Migration 0029 applied to remote D1 BEFORE the worker deploy (5 hats confirmed seeded; order matters - deploying new resolver code before the column exists would 7403/strip cosmetics globally per pre-mortem 1). Smoke: homepage 200, /shop 200, /api/auth/me 401 (clean unauth path, not 500). Auth note: this session the OAuth token was STALE (7403 on D1 query) and the `CLOUDFLARE_API_TOKEN` env var WORKED - the reverse of the old gotcha; kept the token set for wrangler. worker/index.js excluded from the commit (local dev WS-origin patch, regenerated by build). Wave 2 (Barrel Night bracket + crown override + badges-on-monkey, Stages C+D) NOT started; brief calls for a prod bake before it ships. tsc clean, build clean, vitest 182/182 (incl. 3 new hat suites). Branch: `main`, commit: `eb67c0b`.
- **2026-06-13** (5th) - Deployed to nfras4arcade (version `363d19fd-3a8a-439d-aa6c-3ffb2e0abf44`). Summary: Three Nick polish asks after using Phase 3 on prod. (1) Blip reveal slowed 30 -> 18 cps in `chatAudio.ts` and `ChatBubble.svelte` (the bubble's typewriter cadence must match the audio cadence; both consume `revealSchedule(text, cps)`); default in framework-pure `core/chatVoice.ts` left at 30 with a doc comment noting that consumers pass 18. (2) Chat log moved off the rail and onto the 3D stage as a persistent overlay: new `src/lib/table3d/ChatStageLog.svelte` anchored bottom-left of the stage container (`position: absolute; left: 1rem; bottom: 1rem; pointer-events: none; max-width: min(420px, 40%); z-index: 30`), last 6 entries on a dark scrim (`rgba(0,0,0,0.55)`, 10px radius, NO backdrop-filter ever), slide-in 200ms ONCE per entry (no looping), TV variant scaling up (`bottom: 2rem; left: 2rem; max-width: 540px; max 8 entries; font-size clamp(1.1rem, 1.6vw, 1.5rem)`). The in-route rail/classic `<section class="chat-log">` block and its CSS were removed; the stage log is now the single source of truth across all views (table, controller, classic, TV). The bubble overlay above each speaker's monkey stays unchanged. (3) Self-monkey portrait: new `src/lib/table3d/SelfMonkeyPortrait.svelte` in the stage's bottom-right (`right: 1rem; bottom: 1rem; z-index: 25`), 180x200px Threlte canvas with one `PlaceholderMonkey` using the local player's fur colour (computed via `furColourFor(pid, false, EMPTY_MAP)` mirroring the layer's seat colouring exactly), warm key light + subtle fill, authored close-up camera (`[0, 0.5, 2.0]`, FOV 36, lookAt `[0, 0.5, 0]`), thin border + 12px radius, dark background (`rgba(8,10,16,1)`). talkAmplitude is `$derived(Math.max(chatSelfAmp, voiceSelfAmp))` from two reactive sources: chat path sets `chatSelfAmp = 0.5` while the local player has an alive `chatBubbles` entry (0 otherwise), voice path opens an `AudioContext` + `AnalyserNode` (fftSize 512, smoothing 0.4) on `localStream` when `voiceState === 'joined' && !selfMuted`, polls every 10ms via `setInterval`, calls `bandpassRms(buf, sampleRate, 512, 250, 3500)` + `createEnvelope({attack: 0.4, release: 0.15}).update(r)` (identical maths to the remote-jaw `voiceJawDriver` so own-jaw and remote-jaw amplitudes match byte-for-byte), writes `voiceSelfAmp`. The voice $effect's cleanup tears down the interval + disconnects source + closes the AudioContext when voice leaves / selfMuted flips / localStream goes null / the route unmounts, then zeroes `voiceSelfAmp`. Portrait is gated off on TV view and for spectators. Pipeline: one haiku executor under art-director review (1 round caught the missing voice path: the executor implemented chat -> selfAmp only; refactor into `chatSelfAmp` + `voiceSelfAmp` + `$derived` max landed cleanly). Live verified on local stack at 1920x1080 with selfseat on: 3 monkeys at the table, "Guest_3c88: cheering and chatter" entry in the bottom-left stage log, self-portrait visible bottom-right with matching fur colour. tsc clean, build clean, vitest 150/150 (unchanged since Phase 3; no new tests needed for the polish since `audioMeter.ts` is already covered and the changes are wiring). Branch: `main`, commit: `cfba29c`.
- **2026-06-13** (4th) - Deployed to nfras4arcade (version `8ab7668d-096f-4f4b-b491-9aedc4211844`). Summary: Monkey Table Phase 3 (Voice + chat) - the presence layer. Four commits on `feature/table-phase3` shipped together since voice features are dormant until the player taps "Join voice"; chat + Undertale blip is live by default for every liars-dice room. (Stage A) Chat channel: additive `chat` ClientMessage in LiarsDiceRoom (1500 ms per-player in-memory rate limit, 140 char cap, sanitised, seated-player gate, no storage, no alarms) broadcasts back as `player_chat` with a server-stamped ts. Framework-pure `src/lib/table3d/core/chatVoice.ts` derives a deterministic per-speaker VoiceParams from fur colour hash (baseHz 280-540, charDetuneCents +/-12); `buildBlipRecipe` emits a square-wave voice per non-space character via the existing StingRecipe API so the emote-sting AudioContext + `tableMuted` localStorage are reused and the chat blips inherit mute. `ChatBubble.svelte` (visual-only) positions inside `nameplate-layer` via the layer's `platePosMap` (4rem above the speaker's nameplate, threaded as a `chatBubbles` prop + `onchatbubbledone` callback); chat-log panel now visible in EVERY non-TV view (not just classic) so the local speaker always sees their own line in non-self-seat desktop where they have no seat. Single $effect over chatLog plays blips once per new entry across all views; pending blip cancels collected in a Set so concurrent speakers overlap naturally. Compose input in rail + controller + classic, Enter sends, 1500 ms client cooldown matching server limit. 19 unit tests. (Stage B) WebRTC signaling: additive `rtc_signal` case in LiarsDiceRoom that opaquely relays SDP/ICE to a specific target socket via `ctx.getWebSockets(targetTag)`, with per-sender 50/sec rate limit (1000 ms window, in-memory Map). Framework-pure `core/mesh.ts` exports `derivePeerSet` + `diffPeerSet` (12 tests). Browser-only `MeshController.ts`: one RTCPeerConnection per peer with a `keepalive` data channel so the SDP has an m-line (modern browsers reject empty offers); deterministic initiator rule (`selfId < peerId` initiates) plus perfect-negotiation rollback for glare; ICE config uses Google's public STUN (`stun:stun.l.google.com:19302`); zero new alarms/storage/D1. Route lazy-imports + instantiates the controller behind a TV + spectator no-op gate. (Stage C) Voice: MeshController gains `attachLocalStream`/`detachLocalStream`/`getPeerIds` plus `ontrack` per peer (gotStreamFromPeer Set prevents duplicate onRemoteStream); attaching the local stream triggers an automatic re-offer on each initiator's peer connection. Route adds a `voiceState` machine (`off | requesting | joined | denied | unsupported`), getUserMedia behind the Join-voice click (iOS user-gesture rule), self-mute toggles every local track's enabled flag (no renegotiation), per-remote mute persists in `ld-remote-mute` localStorage. `<audio autoplay bind:this>` per peer; an $effect attaches srcObject from the remoteStreams map and reapplies muted from remoteMutes. NotAllowedError -> denied; NotFoundError or absent `navigator.mediaDevices` -> unsupported. Leave stops local tracks so the OS mic indicator drops; cleanup also fires on component unmount. 6 mesh-media tests (one degraded to a behavioural placeholder because `new MediaStream()` is browser-only and not polyfilled in node vitest). (Stage D) Jaw flap from voice: framework-pure `core/audioMeter.ts` with `rms`, `bandpassRms` (speech band 250-3500 Hz selected by bin from sampleRate + fftSize), and a configurable attack/release envelope that caps at 0.6 under prefers-reduced-motion (flap stays informative without becoming manic). Browser-only `voiceJawDriver.ts`: lazy singleton AudioContext on first attach (SSR-safe), MediaStreamAudioSourceNode + AnalyserNode (fftSize 512, smoothingTimeConstant 0.4) per peer, ONE setInterval(10) shared across peers started on first attach and cleared when peers reach zero; close the context on dispose when no peers remain. Wired into the mesh's `onRemoteStream` / `onPeerRemoved` callbacks. `LayerHandle` gains `setRemoteAmplitude(peerId, value)` that writes into `director.talkAmplitudes` so the existing Phase 0 jaw/head-bob math consumes the value with no other changes. 15 audioMeter tests. Architecture decisions parked in `.omc/plans/table-phase3-brief.md`: TURN relay deferred (symmetric-NAT users fall back to chat); Last Words (impostor clip moment) and Punishment Wheel (pitch-shift forfeit) are post-Phase-3 work. Pipeline: 4 haiku executor work orders under art-director review (4 review rounds caught: chat bubble piled centrally instead of per-speaker, classic-view audio gap because the bubble was the only audio source, the local player has no seat in non-selfseat mode and so their own bubble had nowhere to go, a single-cancel pattern that cut the previous speaker's blips when a new speaker arrived). Live verified on local wrangler stack: chat bubble positioned 41% / 22% above speaker, classic log panel mirrors, blip audio plays. Build needed a `.svelte-kit` clean before deploy (stale manifest from interrupted earlier builds is a known gotcha; documented in [[impostor-monkey-table]]). tsc clean, build clean, vitest 150/150 (52 new tests since Phase 2: 19 chatVoice + 12 mesh + 6 meshMedia + 15 audioMeter). Branch: `main`, commit: `539d1d5`.
- **2026-06-13** (3rd) - Deployed to nfras4arcade (version `d133dff4-cc98-419a-ade0-b09428c7fa5a`). Summary: Monkey Table Phase 2 (TV mode, the couch format) + a parallel session's asleep-Zzz polish. Six commits on `feature/table-phase2`, merged to main. (1) Seat layout variants in framework-pure `core/seats.ts`: existing 5-seat `DESKTOP_ARC` stays the untouched default; new `FULL_TABLE_ARC` seats 6 monkeys including the local player on a 150-degree 2.60-radius arc (clearance maths checked: ~1.35 unit chord vs ~1.28 scaled head). `assignSeats(players, myId, prev, { layout?, includeLocal? })` keeps the stability contract for 6 slots. Provisional TV camera datum (`TV_CAMERA_POSITION [0, 1.9, 4.6]`, FOV 46, lookAt `[0, 0.2, -0.5]`) in `core/camera.ts` and the art bible. (2) `LiarsDiceRoom` gains a `role=table` display path: those sockets are never seated, never counted for promotion, never act (existing spectator+tag mechanism reused, hibernation-safe via the existing `[userId, role, socketId]` tag pattern); broadcasts gain additive `tablePresent` derived from `ctx.getWebSockets()` at broadcast time. Zero new alarms here, no D1, no storage. (3) Liars-dice route reads `?role=table` from URL: full-bleed 1920x1080 stage (no rail, no controls), six monkeys via fullTable, TV camera, broadcast-style lower third with current bid + turn, RitualOverlay scaled 1.6x for legibility across a room. Lobby gains a static cast-to-TV `<details>` disclosure: room URL with `?role=table` plus a real QR (reuses existing `QRDisplay.svelte`, no changes). (4) Phone controller layout: when `tablePresent=true` and the client is seated, default to controls-only view (no 3D); resolved once per tablePresent=true session via a non-reactive flag so the broadcast loop cannot stomp an in-session toggle; "Classic view / Full view / Controller view / Full view" toggle next to existing toggles; `ld-controller-view` localStorage persists user override. TV view DEFENSIVELY never enters controller-mode regardless of seating state. (5) Shake-to-roll: new framework-pure `src/lib/table3d/shake.ts` (low-pass baseline + magnitude-delta threshold + cooldown, SSR-safe, iOS `DeviceMotionEvent.requestPermission` flow returning `granted/denied/unsupported/not-required`, passive event listeners, idempotent start/stop). Wired in controller layout only: face-down placeholder dice on each round-cycle (counter bumps on `playing` phase entry, not dice-count-derived because winners keep the same count), tap-to-reveal fallback always present, iOS permission prime button until granted, `prefers-reduced-motion` skips placeholder entirely. Permission lifecycle is a 4-branch state machine (`!controller -> stop+null`, `denied/unsupported -> stop+null`, `unknown -> create without start so requestPermission is reachable`, `granted/not-required -> create + start`); no cleanup-closure-stale bug. 14 unit tests. (6) Self-seat experiment (Nick-judged, no UI toggle yet): `?selfseat=1` query param plus `ld-self-seat` localStorage persistence shows the local player at the table on desktop, using FULL_TABLE_ARC. `?selfseat=0` opts out. TV mode unaffected. (7) Rejoin-no-monkeys fix: real bug found during Nick's hands-on prod acceptance. `handleDisconnect` was wiping bots synchronously via `dropBotsIfNoHumansLeft` the moment the only human's tab closed; a rejoining human within seconds found an empty `players` array, seatMap empty, NO MONKEYS render. Fix: 30s grace window. `handleDisconnect` schedules a bot-drop check via the existing alarm path (using `getAlarm()` to coalesce, mirroring `scheduleReconnectCheck`'s overwrite guard, per the poker DO runaway lesson: zero new alarms without an overwrite check). `handleJoin` clears the pending flag on human rejoin. Alarm handler also clears the flag when everyone is connected by fire-time so a subsequent disconnect can schedule a fresh check (stuck-flag edge case caught in review). `import.meta.env.DEV`-gated console instrumentation on seatMap recomputes and ldState transitions for any future "no monkeys" diagnosis without prod log noise. Verified live in Playwright: 3 bots still seated after a 5-second leave + return on a fresh local stack. (8) Parallel session expression polish (commits `876e4f0` + `d28db12`, prod-deployed earlier as worker `c65dd39f` and re-included in this deploy): bigger brow swings (grin/laugh arch up, shock 0.14, sweat 0.22 pinch + negative offset, smug -0.08, asleep -0.06); cartoon props driven by new `showSweatDrop`/`showAlert` flags on `ExpressionPose` (pale-blue teardrop on right temple for sweat, bold yellow "!" over `AnchorCrown` for shock; pop-in via anticipation/overshoot/settle bell curve per the art bible motion rules; sweat sways 3 Hz, alert bobs 4 Hz; static under prefers-reduced-motion); three yellow Zzz letters parented to AnchorCrown drift up-and-left on a 2.4s staggered cycle (phase offsets 0/0.33/0.67) so the asleep pose reads as sleeping not sad. Pipeline: 4 haiku executor work orders (seats+display, route+controller+cast, shake-to-roll, final-stage selfseat+rejoin) under art-director review (4 review rounds total catching: an `each_key_duplicate` crash on repeated die faces in RitualOverlay, missing controller toggle button + stomping bug on the controller-init effect, dead permission-prime UI + closure-stale lifecycle effect bug, broken deal-detection across same-dice-count rounds, an iOS prime button that called null, TV-view inheriting controller-mode in single-browser test, stuck pending-flag in the grace-window alarm handler). Live 2-tab visual pass on local wrangler stack verified TV view + controller mode + cast QR + rejoin fix + self-seat. Local stack: cleaned `.svelte-kit` after the first build's stale-manifest false start; wrangler deploy needed two attempts to coalesce (first attempt couldn't resolve adapter outputs, second succeeded clean). tsc clean, build clean, vitest 98/98 (45 new tests: 31 seat layouts + 14 shake detector). Branch: `main`, commit: `967d29c`.
- **2026-06-13** (2nd) - Deployed to nfras4arcade (version `c3237a14-93a6-4856-8835-a3040a4c6c85`). Summary: Monkey Table Phase 1 acceptance fixes from Nick's hands-on prod review, plus a day-one liars-dice soft-lock fix found during verification. (1) Liar's Ritual v2: ~3.5s -> ~10.3s for 5 players with an on-stage narrative via new `RitualOverlay.svelte` in the stage overlay stack: LIAR! stamp with caller/accused names, new SHOWDOWN cue (bid + "Counting Xs...", ones-wild noted), reveal cascade at 600ms/player with a running tally counter and per-hand dice glyphs (matches gold), new HOLD suspense cue ("N found... M needed"), verdict stamp LIAR CAUGHT! (crimson) / NOT A LIE! (green) + "{loser} loses a die", then restore. Tally maths (ones-wild aware) computed in framework-pure `core/ritual.ts`; banner state on TableDirector; reduced motion unchanged (skip to verdict, no banner). Director frame delta clamped to 0.1s so tab-away rAF suspension cannot fast-forward an in-flight ritual (also why headless screenshot QA misses rituals: sample in-page instead; verified live by 150ms in-page sampling, timings within ~50ms of spec). (2) Monkeys scaled to 0.62 (`MONKEY_SCALE` in core/seats.ts) on a deeper 2.30 arc; five heads now clear each other (was severe overlap, heads ~2 units wide vs ~1.2 unit chord); nameplate anchor scales; art bible staging datum updated. (3) Rail fix: Nick's "dice go off screen" was the FACE PICKER, not the your-dice row: global button padding (0.75rem 1.5rem) puts a 49.6px border-box floor under each of the 6 face buttons (382px of content in the 290px rail row); fixed with flex sizing + zeroed horizontal padding, scoped `.table-mode` only, classic pixel-identical. (4) Soft-lock fix (pre-existing since liars-dice day one): `bidCount` never clamped when totalDice shrank between rounds; a stale over-ceiling count made bumpCount(-1) compute a still-over-ceiling value the guard rejected, `+` disabled, and with no current bid Call Liar disabled: the round opener had NO legal action and the game died. Decrements now always allowed; bid-sync $effect clamps to the live ceiling. Reproduced and verified fixed on a local wrangler stack. (5) Mouth-line removal merged (`596d601`, Nick approved from /dev/monkey screenshots: chin/jaw carry expression, shock gains a real mouth cavity). Pipeline: 3 haiku executor work orders under art-director review (ritual: 1 fix round incl. an each_key_duplicate crash on repeated die faces; rail: 2 rounds, root cause found by live cascade inspection), 2 one-line direct fixes. Local stack recipe: `bun run build` then `bunx wrangler dev --port 8787` (client WS hardcodes 8787; worker/index.js is a tracked GENERATED artifact: never run patch-worker.ts twice without a fresh vite build, duplicate-symbol esbuild failure). tsc clean, build clean, vitest 53/53 (was 37; ritual timeline + tally tests added). Branch: `main`, commit: `e03db70`.
- **2026-06-13** - Deployed to nfras4arcade (version `49efc2bb-68ef-40e9-b783-4d8fda866b44`). Summary: Monkey Table Phase 1: a 3D presence layer for liars-dice. Merge of `feature/table-phase0` (8 commits, Waves A-E built in an isolated worktree by a Sonnet executor under art-director review) plus `feature/monkey-head-v2` (parallel session's rounder head with dimensional mouth). Liars-dice gains an optional Table view (`ld-table-view` localStorage key, default ON at >=900px with WebGL, classic view pixel-identical and one click away): up to five procedurally built monkeys seated around a barrel table (Threlte 8.5 + three 0.184, all client-side in lazy chunks, worker bundle untouched), state-driven moods (turn perk, disconnect slump, eliminated asleep), seated mouse parallax, and the Liar's Ritual on every liar call (house lights to 12 percent, twin gold/orange spotlights isolating caller and accused, staggered reveal cascade, shock/laugh verdict; reduced-motion skips to the verdict). Emote soundboard: six emotes as portable data (expression + synthesised Web Audio sting recipes, no audio files, mute key `tableMuted`), relayed via the phase's ONE protocol change: an `emote` case in LiarsDiceRoom (seated players, lobby/playing/round_over phases, 1-per-2s in-memory rate limit, silent drops, no storage/alarms/D1) broadcasting `player_emote`. Architecture: engine-agnostic `src/lib/table3d/core/` (rig pose data, seat math, semantic event derivation, ritual timeline, emote registry; zero framework imports, 37 vitest tests via `vitest run`) with thin Threlte interpreters, per the Steam-port boundary in `docs/table-porting.md`; art contract in `docs/table-art-bible.md` (notable canon: idle UI is static, motion belongs to events). Toolchain: vite-plugin-svelte 4.0.4 -> 5.1.1 (v4 does not support vite 6; the mismatch broke esm-env condition resolution so `$app/environment` dev was false in-browser, and duplicated the Svelte runtime crashing Threlte context; likely the historic root cause of the onMount tree-shaking quirk documented below: worth retesting onMount someday). patch-worker.ts origin guard now admits localhost origins only when the request host is itself localhost, so wrangler dev works without hacks. RUN `bun install` AFTER PULLING (plugin upgrade). Dev harnesses at /dev/monkey and /dev/table (import.meta.env.DEV gated). Known follow-ups: Wave E one-viewport layout (side rail at >=1200px, zero page scroll) verified statically only, pending Nick's hands-on acceptance; sting audio unreviewed; parallel session has uncommitted monkey WIP in the main checkout on `feature/monkey-head-v2`. Deploy auth note: a limited CLOUDFLARE_API_TOKEN env var shadows the OAuth login and 10000-errors deploys; clear it for wrangler deploy. tsc clean, build clean, vitest 37/37. Branch: `main`, commit: `c7e56be`.
- **2026-06-12** (2nd) - Deployed to nfras4arcade (version `e3c237ee-041c-4e08-904a-538fbd091d45`). Summary: HOTFIX + store discoverability. Live guest playtest (Playwright agent) caught connect-four rooms hard-crashing with effect_update_depth_exceeded: VFX prev-value trackers declared as $state were read inside their own $effect then unconditionally reassigned fresh objects each run, so the effect retriggered forever and the room died at "Connecting...". All nine prev-trackers across connect-four/liars-dice/blackjack/roulette/chase-the-queen/president converted to plain lets (the kit's USAGE.md prescribed pattern; primitive-typed ones had converged by luck, object-typed ones were live or latent crashes). Lesson: tsc/build gates cannot catch reactive-loop runtime bugs; a cheap browser playtest can. Also shipped: hub "Arcade Store" strip (casino-gold clip-path banner between category grid and Live Now, SAVE 59% chip, price-tag SVG) fixing /store having zero inbound links. Branch: `main`, commit: `f3b728e`.
- **2026-06-12** - Deployed to nfras4arcade (version `69ab4773-f69c-4436-a5a6-5c25d1646092`). Summary: VFX/juice pass across all 12 games, completion deploy. Shared kit at `src/lib/vfx/` (vfx.css utility classes vfx-shake/-slam-in/-pop-in/-breathe/-flash-*/-sparkle-text/-dissolve-out + keyframes vfx-tumble/-deal-arc/-ring/-float-up; Shockwave/FloatUp/Spotlight components; burst.ts gold/loss confetti wrappers; USAGE.md cookbook) imported once in +layout. Per-game effect hooks (presentation-only, no logic/WS changes, runes + prev-value transition detection): impostor role-flip + spotlight reveal + CAUGHT/ESCAPED stamps + glitch flicker + word letter cascade; president pile shockwaves + QUAD shake + coronation sparkle; chase-the-queen queen vignette + trick banner slam + MOON SHOT + score FloatUps; connect-four squash-stretch drops + sequential win-line ignite; wavelength dial target unveil + BULLSEYE moment; snap speed-lines + slap shake + champion banner; liars-dice full treatment (tumble reveal cascade, match ignite, LIAR! slam, dissolving lost dice, pot FloatUps); coup crosshair flicks + challenge spotlight + influence burn; blackjack BLACKJACK/BUST stamps + payout floats; roulette spin motion-blur + green 14x mega-moment; baccarat winner glow wall + NATURAL shimmer; slots tiered 5x/15x JACKPOT celebrations + payline draw-in. Built by a 26-agent Workflow (12 sonnet builders w/ per-game work orders + strict file ownership, 12 haiku QA, kit + gate agents). NOTE: this work collided with the parallel security-audit session (its 09:41 C4 + 10:02 consolidation deploys shipped most VFX mid-recovery); the session stash-trampled in-flight work, dropped liars-dice entirely (recovered from dangling stash commit ca5ec7d via git fsck), and its consolidation commit f52f6c3 carried two timer-hygiene bugs fixed here (president combo-hide effect, chase-the-queen FloatUp forEach no-op cleanup). Lesson recorded: one session per repo; use worktree isolation for write-heavy workflows. tsc clean, build clean. Branch: `main`, commit: `833f6d7`.
- **2026-06-11** - Deployed to nfras4arcade (version `7780f3f7-82cf-4153-b4b5-d57185f68730`). Summary: New customer-facing `/store` bundle storefront (single new file `src/routes/store/+page.svelte`, ~990 lines, nothing else touched). Bento grid selling the 8 party games as packs: Full Party Pack hero 2x2 tile ($31.92 struck to $12.99, SAVE 59%, rotated BEST VALUE ribbon, looping shine sweep), Bluffer's Bundle (Impostor + Liar's Dice + Coup, $8.99, blue), Card Night (President + Chase the Queen + Snap, $8.99, yellow), Head-to-Head (Connect 4 + Wavelength, $5.99, casino orange), animated savings-meter tile, stats tile, promise tile with inline SVG glyphs, full-width solo strip ($3.99 chips with type-color glow). Static draft: no checkout backend, buy CTAs fire a coming-soon toast (role=status, aria-live). Per-tile accent theming via element-level CSS custom props (--tile-accent/--tile-faint/--tile-border) over the standard clip-path + ::before gradient-border tile base; the border gradient's z-index:0 produces a translucent accent wash per tile face, kept deliberately. Light mode + mobile verified by screenshot; ribbon text uses var(--bg) so it stays readable in both themes. Built via Workflow orchestration (sonnet builder + 2 haiku QA + sonnet check-runner, Opus PM review pass caught duplicate copy on Bluffer's title, hardcoded ribbon hex, role=listitem on button, dead .tile-strip selector masking a stacking gap). svelte-check clean for the route, tsc clean, build 30s. Branch: `main`, commit: `bd8d382`.
- **2026-05-12** - Deployed to nfras4arcade (version `f657bf2b-dbd6-4c47-9dac-dd443a671989`). Summary: Formal retirement of Texas Hold'em poker. UI + docs only per chosen scope. `/poker` page now shows a permanent retirement notice with a link to `/casino` (replacing the prior "temporarily disabled while we investigate" message that had been live since the 2026-05-01 kill switch). `/casino` removed the greyed-out poker tile entirely (was a confusing dead row). Hub Casino card description: "Blackjack, Roulette, Baccarat & more" / "4 Games" (was 5). Server code stays dormant: `worker/poker/` lives in repo for archaeology, DO binding kept in wrangler.jsonc, `/ws/poker` still returns 503. Reversible if poker is ever re-enabled (flip `POKER_DISABLED`, restore casino entry, undo count). Chip economy untouched: blackjack/roulette/baccarat all use the same chips column on player_profiles, so historical poker winnings stay meaningful. Phase 7 (3D rendering) brief at `.omc/plans/phase7-kickoff-brief.md` is now obsolete in its current form (was poker-scoped). Branch: `main`, commit: `b3f11b6`.
- **2026-05-12** - Deployed to nfras4arcade (version `77b93cc9-478c-4f58-aa3a-2c5378b3667d`). Summary: Lobby ghost-player bug fix + mobile UI sweep, 14 files changed (301 insertions, 20 deletions). Root cause of the long-standing "players are in the lobby but don't appear until refresh" complaint: `CardRoom.loadState()` at `worker/cards/cardRoom.ts:161-166` and `CasinoRoom.loadState()` at `worker/casino/casinoRoom.ts:93-95` unconditionally marked every non-bot player `connected:false` when the Durable Object woke from hibernation. The WebSocket Hibernation API keeps sockets alive across hibernation, so this is wrong: only the player whose message woke the DO got re-marked connected, and the subsequent broadcastState told every other client they were disconnected. ImpostorRoom, SnapRoom, WavelengthRoom, LiarsDiceRoom already reconciled correctly against `ctx.getWebSockets()`; only the CardRoom + CasinoRoom hierarchies had the bug. Fix mirrors the reference pattern at `worker/impostor/room.ts:118-132`. Affected games: President, Chase the Queen, Connect 4, Coup (via CardRoom); Blackjack, Roulette, Baccarat (via CasinoRoom). Secondary fix: `play_again` / `resetToLobby` now prunes disconnected players in CardRoom (line 326-352), CasinoRoom (line 252-266), Snap (line 1015-1039), Wavelength (line 1348-1376), LiarsDice (line 957-970). Only ImpostorRoom (line 1248-1253) had this correct. LiarsDice also gains spectator promotion in resetToLobby (was missing entirely, every other game had it) and disconnectTimestamps clearing. Mobile UI sweep across 8 files: profile badge tooltips now tap-toggleable with outside-click + Escape handlers (were `:hover`-only, completely invisible on touch); profile/friends/FeedbackWidget input font-size raised to 1rem to stop iOS auto-zoom (had been 0.875rem-0.9rem); FeedbackWidget mobile trigger moved from bottom-left to bottom-right to avoid corner-about overlap; admin feedback sort buttons get 44px min-height tap target; leaderboard winners table wrapped in overflow-x:auto; liars-dice in-game gets safe-area-inset-bottom for iPhone home indicator; snap `100vw` to `100%` (scrollbar-induced desktop overflow); about highlight-grid `minmax(280px, 1fr)` to `250px` for 375px viewport. Deferred: global `button { clip-path }` change as too risky for the scope. Audits filed at `.omc/plans/lobby-audit.md` and `.omc/plans/mobile-audit.md`. Pipeline: two parallel architect agents produced punch lists, two parallel opus executors applied fixes, code-reviewer agent gave APPROVE-WITH-MINOR-FIXES (the one MEDIUM follow-up applied in same commit), test-engineer added `tests/lobby-hibernation.test.ts` (4 tests) and verified by revert-and-rerun that the test catches the regression. tsc clean, build clean. Branch: `main`, commit: `aa0029d`.
- **2026-05-10** - Deployed to nfras4arcade (version `ce2a0fd1-2a0b-4a13-9c6e-56991d78133f`). Summary: Auth fix bundle. PBKDF2 iterations 600k -> 210k (600k overran CF Workers' 50ms CPU budget on register, surfacing as opaque 500s; verifyPassword's prefix-aware decode keeps older 600k hashes valid). Rate limiter split into `peek`/`record` so validation errors don't burn buckets; register loosened to 100/hr per IP + 20/hr per email (was 3/hr per IP); login loosened to 30/min and now only counts FAILED attempts so a busy NAT can keep signing in. Client `auth.ts` falls back to response.message when `error` is missing, so SvelteKit framework errors surface instead of generic 'Registration failed'. .gitignore adds Obsidian junction artifacts (.obsidian/, .trash/, Trash/, *.tmp, .obsidian.vimrc) so cross-mounted vault files stay out of git. Build 51.92s, tsc clean. Branch: `main`, commit: `72fa5ac`.
- **2026-05-01** - Deployed to nfras4arcade (version `bb306fc7-9542-4d2c-8df4-a28971a865a7`). Summary: Disable poker end-to-end behind a kill switch after Durable Object usage runaway. Worker patch rejects `/ws/poker` upgrades before the DO is touched; `PokerRoom.fetch` returns 503 for all traffic and `alarm()` drains pending alarms without rescheduling so the room-expiry watchdog stops billing; `webSocketMessage` force-closes leftover hibernated sessions; `/api/{create-solo,add-bot,remove-bots}` reject `game=poker` with 503; `/poker` page short-circuits to a "temporarily disabled" notice instead of opening a socket; casino hub greys out the Poker game card (no link, disabled "Unavailable" button, desaturated, pointer-events disabled) and the entire Open Tables list (rows disabled, heading "Open Tables (unavailable)"). tsc clean, build clean. Branch: `main`, commit: `f998f6c`.
- **2026-04-30** - Deployed to nfras4arcade (version `b7867317-994b-4373-947f-e23f2558416d`). Summary: Phase 6 of the paired-device migration (UI polish + cleanup). 19 in-scope items shipped across 4 waves. Wave A1 titleText sweep across 11 non-poker game DOs (snap, president, chaseTheQueen, connectFour, coup, blackjack, roulette, baccarat, wavelength, liarsDice, impostor) plus casino type/resolver chain (CasinoPlayer interface + casinoRoom resolver line) and src/lib/types.ts shared Player interface; consistent emission pattern `titleText: p.titleText ?? null` next to titleBadgeId mirrors the Phase 5 poker reference. Wave A2 CSS-only animations P6-2 (chip slide-in 300ms via reactive bet diff and seatXY-based --from-x/--from-y CSS vars), P6-3 (dealer-button slide ~600ms with separate floating "D" token and seat math, transition on transform), P6-4 (showdown card flip 200ms rotateY with 100ms staggered delay via animation-delay from card index). All animation classes use phase6- prefix to avoid collision; all gated by prefers-reduced-motion. Wave B cleanup: deleted /api/pair/remember and /api/pair/check_remembered route files, scrubbed `remembered` state + `$effect` fetches + template conditionals + CSS in both src/routes/pair/+page.svelte and src/lib/components/pairing/PairButton.svelte (the second consumer was an architect-flagged miss in the original brief), deleted orphaned src/lib/components/pairing/deviceFingerprint.ts (zero remaining imports after Wave B), added unmount-cleanup `$effect(() => () => clearTimeout(dropoutHideTimer))` at src/routes/poker/[code]/+page.svelte (P6-17 retargeted from DropoutToast.svelte per recon - the toast component already cleans its own interval), confirmed P6-18 casino spectator promotion gate is a no-op (no canPromoteSpectator path exists in worker/casino/), P6-19 PlayerSeat min-width override deferred pending real-device QA. Wave C P6-1 paired_fold_animation: new WebSocket message type emitted via `this.broadcast(...)` (the actual cardRoom.ts:1129 method, replacing nonexistent broadcastToAll/hasPairedDevice from initial draft) immediately after fold mutation in BOTH the case 'fold' branch (worker/poker/room.ts:350) AND handlePlayerTimeout (line 878), positioned BEFORE each early-return path so the animation fires even when fold ends the hand; client-side 3-layer wiring chain in src/routes/poker/[code]/+page.svelte (`tick` import + `foldAnimation = $state<{playerId, fromSeat} | null>(null)` + async onMessage handler with `await tick()` guard at line 89 to prevent Svelte 5 reactive batching from coalescing the animation trigger and subsequent state_update into a single DOM flush) plus prop pass-through `foldAnimation={foldAnimation} onfoldAnimationDone={() => { foldAnimation = null; }}` to both TableView and MobilePokerView; views render a transient phase6-fold-arc card-back element animated from seat to muck pile center over 500ms, with `onanimationend` invoking the callback plus a 500ms setTimeout fallback inside `$effect` so reduced-motion users (animation: none) still clear foldAnimation. Pipeline: existing kickoff brief at .omc/plans/phase6-kickoff-brief.md → ralplan consensus iteration 2 (Architect + Critic both APPROVE after 9 fixes applied across 1 iteration including replacing phantom method names, specifying full client wiring chain, adding await tick() guard, expanding cleanup scope to PairButton.svelte, real-player resolver lines for 4 standalone DOs, wave restructure, ADR honesty correction, fold-animation clearing mechanism, and emit-before-early-return positioning) → autopilot Phase 2 execution (3 parallel executors for waves A1+A2+B then sequential opus executor for wave C with await tick guard correctness as the load-bearing constraint) → Phase 4 validation (architect APPROVE on functional completeness across 7 checks; security-reviewer APPROVE with confirmation that titleText is admin-curated D1-sourced and Svelte-escaped not {@html}'d, that paired_fold_animation leaks no data beyond what broadcastState already broadcasts, that device_pairings table retention is intentional per logout cleanup at api/auth/logout/+server.ts:16, and that no new D1 mutations or auth paths were introduced; code-reviewer APPROVE-WITH-MINOR-FIXES → all 5 fixes applied in a follow-up pass: $derived → $derived.by for arrow-function-bodied derived values to restore reactivity tracking [TableView lines 154/173 plus 4 template call-site removals], dead `toRemove` variable removal, dunder __foldSeat/__timeoutFoldSeat renamed to plain locals, orphaned deviceFingerprint.ts deletion, and one explanatory "what" comment removed). Final test baseline preserved exactly: 339 pass / 28 pre-existing fail in server/ (unrelated standalone Bun impostor server). Build 29.23s, tsc clean. Phase 7 (3D rendering update) gated on real-play feedback that 2D table view feels flat; kickoff brief at .omc/plans/phase7-kickoff-brief.md needs deep-interview before ralplan. Branch: `main`, commit: `019cb8c`.
- **2026-04-30** - Deployed to nfras4arcade (version `f69204ee-07d6-477e-96ca-c295858f4cff`). Summary: Phase 5 of the paired-device migration (polish + correctness invariants + tests). 16 items shipped across 5 waves: spectator buy-in atomic D1 gate via new `canPromoteSpectator` hook on CardRoom (default true; PokerRoom override does atomic `UPDATE chips = chips - ? WHERE chips >= ?` with `meta.changes` guard for insufficient funds; casual mode short-circuits with no D1 round-trip; error toast via existing `sendTo`); `scheduleBotTurn` alarm overwrite guard mirroring `scheduleDisconnectCheck` pattern so a sooner disconnect/grace alarm is never clobbered; `titleText` resolver via LEFT JOIN badges in `cosmetics.ts` (server-side; bots get null) plumbed through types.ts to PlayerSeat across TableView + MobilePokerView, fixing a pre-existing bug where opponents had no title text; surface dropout grace toast UX (new DropoutToast.svelte using Svelte 5 runes countdown via $effect interval, position fixed, single page-level mount; cardRoom emits `paired_grace_expired` inside the `if (stillEmpty)` branch after `handleDisconnect`; `paired_device_removed` payload includes `graceDurationMs` so the client uses authoritative server duration; page handlers gate on `roleParam` in `{controller,table}` so non-paired users never see false toasts); HoleCards muck-arc `'offscreen-top'` wiring (extended `isReleaseOverMuck` to commit fold when pointer release is in upper 30% of viewport for paired-mode controller throw-up gesture); hidden the "Remember 7 days" pair checkbox + state + POST + orphaned CSS (backend `device_pairings` table kept for future re-enable; endpoint deletion deferred to Phase 6); `Object.hasOwn` fix on `betPlayers.eliminated` derivation prevents flash of all-eliminated opponents for fresh spectators; removed dead `{#if !state}` block in MobilePokerView (parent already gates); PlayerSeat 150ms transition documented as intentional; `.armed-dimmed` CSS deduped to global `app.css` utility. 13 new tests in `worker/poker/__tests__/{fairness,alarmGuard,cleanup}.test.ts` + `src/routes/api/pair/__tests__/pair.test.ts`: fairness invariant asserts `union(controller, table) === both` with no field-conflict assertion (treats both `null` and empty arrays as "stripped" to catch truncated-hand divergence); alarm guard tests sooner-alarm-preserved with deterministic 200ms/5s comparison; cleanup tests device-removal accounting + auto-fold timeout pipeline; pair tests auth-mismatch 403 + happy 200 + mismatch-does-not-consume + expired. Pipeline: brief at `.omc/plans/phase5-kickoff-brief.md` (16 items P5-1 through P5-16) → ralplan consensus iter 1 ACCEPT-WITH-RESERVATIONS (5 critic clarifications applied inline) → autopilot Phase 2 execution (one opus executor across 5 waves) → Phase 4 validation (architect APPROVE on chip atomicity + fairness invariant; code-reviewer COMMENT with 1 HIGH dropout-toast partner-inference guard + 3 MED tests-and-docs all fixed inline). Wave 6 manual QA items (P5-3 bot rendering on TableView, P5-12 mobile lobby callback binding, P5-16 slow-network redirect verification) deferred to post-deploy. Phase 6 (UI polish + cleanup; 19 items, ralplan-direct ready) and Phase 7 (3D rendering update; needs deep-interview) kickoff briefs filed at `.omc/plans/phase{6,7}-kickoff-brief.md`. Build 25.69s, tsc clean, 339 pass / 28 pre-existing fail (server/ baseline maintained, +12 new tests since prior deploy). Branch: `main`, commit: `22b8fc4`.
